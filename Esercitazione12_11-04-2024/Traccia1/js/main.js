// Benissimo

let numero = parseInt(prompt("Inserisci un numero per far apparire la tabellina"))

for (let index = 1; index <= 10; index++) {

    console.log(`${numero} x ${index} = ${index * numero}`);

}